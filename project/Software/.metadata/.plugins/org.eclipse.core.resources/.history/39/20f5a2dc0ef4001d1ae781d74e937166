#include <stdio.h>
#include ".\spectrum_comm\DMA\dma_hal.h"

int main() {
	int ret;

	DMA_init();

	ret = DMA_request(10);
	if (ret != 0) {
		switch (ret) {
		case TIMEOUT:
			printf("DMA request timeout");
			break;
		case ALREADY_DONE:
			printf("DMA already obtained");
			break;
		}
	}

}
