#include "../../terasic_lib/terasic_includes.h"
#include "alt_types.h"  // alt_u32
#include "io.h"
#include "system.h"

// Control Bus (RDn, WRn, MEMREQn, IOREQn)
#define READ_LINE(x)		not (x<<3)
#define WRITE_LINE(x)		not (x<<2)
#define MEM_REQ_LINE(x)		not (x<<1)
#define IO_REQ_LINE(x)		not x

#define CTRL_BUS_SET(x)		IOWR_ALTERA_AVALON_PIO_DATA(CTRL_BUS_BASE, x)
